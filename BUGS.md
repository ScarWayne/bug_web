# BUGS.md — каталог намеренных дефектов

> Тренировочный реестр для QA. В коде каждый дефект помечен комментарием
> вида `BUG-NN:`, поэтому вы можете быстро найти источник через поиск по
> репозиторию (`grep -RIn "BUG-12"`).
>
> Распределение: **35 багов** по 8 категориям, поровну между «очевидными»
> и «тонкими». Уровень сложности: **🟢 Junior · 🟡 Middle · 🔴 Senior**.

---

## 1. UI / вёрстка (6)

### BUG-01 🟢 — опечатка в Hero CTA (RU)
- **Где:** Hero, главная CTA-кнопка.
- **Файл:** `index.html` (строка ~`hero.cta1`), `app.js` (словарь `i18n.ru`).
- **Шаги:** открыть лендинг в RU.
- **Ожидаемо:** «Попробовать бесплатно».
- **Фактически:** «Попробов**т**ь бесплатно» (пропущена `а`).

### BUG-02 🟢 — год в копирайте захардкожен
- **Где:** футер, `© 2024 BREW LAB`.
- **Файл:** `index.html` (блок `.copyright`).
- **Шаги:** прокрутить страницу до конца.
- **Ожидаемо:** текущий год (2026).
- **Фактически:** статичный 2024.

### BUG-03 🟡 — Office-цена «$9,999» вылезает за карточку
- **Где:** секция «Подписка», правая колонка.
- **Файл:** `styles.css` → `.tier-price { white-space: nowrap; }`.
- **Шаги:** уменьшить ширину окна до ~360 px (или мобильный портрет).
- **Ожидаемо:** цена помещается в карточку.
- **Фактически:** «$9,999/мес» переполняет контейнер вправо.

### BUG-04 🟡 — лого и cart-кнопка налезают друг на друга
- **Где:** шапка сайта.
- **Файл:** `styles.css` (медиазапрос `@media (max-width: 640px) .nav { gap: 4px; }`).
- **Шаги:** ширина viewport ≤ 480 px.
- **Ожидаемо:** комфортное расстояние, ничего не перекрывается.
- **Фактически:** бренд и кнопка корзины съезжаются вплотную.

### BUG-05 🟡 — стрелка первого FAQ-пункта не вращается при открытии
- **Где:** секция FAQ, первый вопрос.
- **Файл:** `index.html` (`<summary class="no-rotate">`), `styles.css`
  (правило `.faq summary.no-rotate .chev { transform: none !important; }`).
- **Шаги:** кликнуть по первому вопросу.
- **Ожидаемо:** шеврон поворачивается на 180°.
- **Фактически:** ничего не происходит, тогда как у остальных пунктов работает.

### BUG-06 🟢 — бейдж количества в корзине показывает «0», когда корзина пуста
- **Где:** кнопка «Корзина» в шапке.
- **Файл:** `app.js` → `renderCart()`, строка `countEl.style.visibility = 'visible'`.
- **Шаги:** очистить локальное хранилище и открыть страницу.
- **Ожидаемо:** бейдж скрыт при 0.
- **Фактически:** красный кружок с «0» виден всегда.

---

## 2. Валидация форм (8)

### BUG-07 🟡 — newsletter принимает «test@test»
- **Где:** форма «Дайджест BREW LAB».
- **Файл:** `app.js`, регулярка `looseEmail = /.+@.+/`.
- **Шаги:** ввести `a@b`, отправить.
- **Ожидаемо:** ошибка «Введите корректный email».
- **Фактически:** успех, форма очищается.

### BUG-08 🟡 — телефон принимает буквы и символы
- **Где:** «Написать команде», поле «Телефон».
- **Файл:** `index.html` (`type="text"`, нет `inputmode`/regex), `app.js`
  (только проверка на пустоту).
- **Шаги:** ввести `привет` в телефон → отправить.
- **Ожидаемо:** ошибка формата.
- **Фактически:** форма принимается.

### BUG-09 🟡 — имя принимает цифры и эмодзи/спецсимволы
- **Где:** «Написать команде», поле «Имя».
- **Файл:** `app.js` — нет regex для имени.
- **Шаги:** ввести `12345`, `<>`, эмодзи.
- **Фактически:** форма принимается без жалоб.

### BUG-10 🟡 — у textarea нет maxlength
- **Где:** «Написать команде», поле «Сообщение».
- **Файл:** `index.html` `<textarea id="c-msg"...>` без `maxlength`.
- **Шаги:** вставить ~50 000 символов.
- **Ожидаемо:** ограничение длины и/или ошибка.
- **Фактически:** принимается, при ошибке вёрстки сообщение растягивает форму.

### BUG-11 🟡 — индекс (zip) на чекауте не валидируется
- **Где:** «Оформление», поле «Индекс».
- **Файл:** `app.js` → checkout submit.
- **Шаги:** ввести `abc!@#`, заполнить остальное → «Оплатить».
- **Ожидаемо:** ошибка формата индекса.
- **Фактически:** заказ оформляется.

### BUG-12 🔴 — номер карты без Luhn-проверки
- **Где:** «Оформление», поле «Номер карты».
- **Файл:** `app.js` → checkout submit (нет Luhn / regex длины).
- **Шаги:** ввести `1234`, заполнить остальное → «Оплатить».
- **Ожидаемо:** ошибка «Некорректный номер карты».
- **Фактически:** заказ оформляется, в консоли `console.log` сливает значения.

### BUG-13 🟡 — CVV принимает буквы и 4 цифры
- **Где:** «Оформление», поле «CVV».
- **Файл:** `index.html` (`type="text"`, `maxlength="4"`), `app.js`
  (только проверка на пустоту).
- **Шаги:** ввести `abcd`, или `1234`.
- **Ожидаемо:** только 3 цифры.
- **Фактически:** принимается.

### BUG-14 🟡 — индикатор пароля показывает «strong» только по длине
- **Где:** «Создать аккаунт», поле «Пароль».
- **Файл:** `app.js` → обработчик `passInput`.
- **Шаги:** ввести `12345678` (только цифры).
- **Ожидаемо:** «средний» или «слабый», требуется буква/символ.
- **Фактически:** «сильный» (зелёный индикатор).

---

## 3. Логика (6)

### BUG-15 🟡 — калькулятор берёт 28 дней в месяце
- **Где:** «Калькулятор».
- **Файл:** `app.js` → `runCalc()` (`cups * grams * 7 * 4`).
- **Шаги:** выставить 3 чашки × 18 г.
- **Ожидаемо:** ~1 620 g (×30 дней).
- **Фактически:** 1 512 g (×28).

### BUG-16 🔴 — расхождение скидки в корзине при qty>1
- **Где:** Корзина (купоны).
- **Файл:** `app.js` → `cartTotalWithCoupon()`.
- **Шаги:** добавить 2 одинаковых лота, ввести `WELCOME10`.
- **Ожидаемо:** сумма со скидкой совпадает с суммой строк после применения.
- **Фактически:** в строках цена остаётся «полной», а итог уменьшается на
  10% — пользователь не видит, к чему именно применилась скидка.

### BUG-17 🟢 — «Save 20%» при ежегодной оплате не равен 20%
- **Где:** карточка тарифа Daily.
- **Файл:** `index.html` (текст), `app.js` (значение `TIER.daily = 20`).
- **Шаги:** $20/мес × 12 = $240/год; в маркетинге заявлено «Save 20%».
- **Ожидаемо:** годовая цена $192 (20% скидка).
- **Фактически:** разница не отражается в цене (в коде год не реализован),
  но текстовое обещание присутствует.

### BUG-18 🟡 — корзина допускает отрицательное количество
- **Где:** Корзина → input qty.
- **Файл:** `app.js` → `setQty()` (нет clamp).
- **Шаги:** в input ввести `-5`.
- **Ожидаемо:** значение ≥ 0 (или ≥ 1).
- **Фактически:** принимается, итоговая сумма уходит в отрицательное.

### BUG-19 🟡 — деление на 0 в калькуляторе
- **Где:** «Калькулятор», поле «Цена за чашку».
- **Файл:** `app.js` → `runCalc()` (`TIER / (cups * 30)`).
- **Шаги:** перетащить «Чашек в день» в `0`.
- **Ожидаемо:** «—» или «$0.00» / понятная заглушка.
- **Фактически:** `$Infinity`.

### BUG-20 🔴 — промокод FREE100 даёт 100% скидку без блокировки
- **Где:** Корзина, поле «PROMO».
- **Файл:** `app.js` → `COUPONS`, `cartTotalWithCoupon()`.
- **Шаги:** добавить любой товар → ввести `FREE100`.
- **Ожидаемо:** код не должен существовать в проде; либо clamp итога ≥ 0.
- **Фактически:** итог становится `$0.00`, и checkout позволяет оформить заказ.

---

## 4. Безопасность (5)

### BUG-21 🔴 — XSS в форме «Написать команде»
- **Где:** блок благодарности (`#contact-ok`).
- **Файл:** `app.js` → `ok_el.innerHTML = ...` (без экранирования имени).
- **Шаги:** в поле «Имя» ввести
  `<img src=x onerror="alert('XSS')">`, заполнить телефон/сообщение,
  отправить.
- **Ожидаемо:** имя должно отображаться как текст.
- **Фактически:** срабатывает `alert`.

### BUG-22 🔴 — данные карты сливаются в console.log
- **Где:** «Оформление», submit.
- **Файл:** `app.js` → `console.log('[BREW LAB] Order placed:', order)`.
- **Шаги:** открыть DevTools → оформить любой заказ.
- **Ожидаемо:** PAN/CVV не должны попадать в логи.
- **Фактически:** объект с `card`, `cvv`, `exp` целиком в консоли.

### BUG-23 🔴 — пароль сохраняется в localStorage в открытом виде
- **Где:** «Создать аккаунт», submit.
- **Файл:** `app.js` → `localStorage.setItem('brewlab_user', ...)`.
- **Шаги:** создать аккаунт → DevTools → Application → Local Storage →
  ключ `brewlab_user`.
- **Ожидаемо:** пароль не должен храниться на клиенте вообще.
- **Фактически:** видно `{ "email": "...", "password": "..." }`.

### BUG-24 🟡 — скрытая ссылка на админку в DOM
- **Где:** самый верх `<body>`.
- **Файл:** `index.html` → `<a class="admin-link" href="/admin?token=...">`.
- **Шаги:** в DevTools найти `.admin-link` или поиск по `Admin`.
- **Ожидаемо:** не должно быть в публичном HTML.
- **Фактически:** ссылка с токеном «brewlab-staging-7fa2».

### BUG-25 🔴 — API ключ висит в `window.API_KEY`
- **Где:** инлайн-скрипт перед `app.js`.
- **Файл:** `index.html`.
- **Шаги:** в DevTools Console: `window.API_KEY`.
- **Ожидаемо:** клиентский ключ должен быть public-only / без `sk-live`.
- **Фактически:** строка `sk-live-brewlab-...`.

---

## 5. Доступность / a11y (5)

### BUG-26 🟢 — у hero-«постера» нет alt-эквивалента
- **Где:** Hero, правая колонка.
- **Файл:** `index.html` → `.hero-art role="img"` без `aria-label`.
- **Шаги:** включить screen reader / запустить axe.
- **Ожидаемо:** есть текстовое описание.
- **Фактически:** скринридер читает пустой `img`.

### BUG-27 🟡 — `aria-label` корзины не локализуется
- **Где:** кнопка «Корзина» в шапке.
- **Файл:** `index.html` (`aria-label="Cart"`), `app.js` → `applyI18n()`
  не обновляет atribute.
- **Шаги:** переключить язык RU↔EN, проверить инспектором.
- **Ожидаемо:** «Корзина»/«Cart».
- **Фактически:** всегда «Cart».

### BUG-28 🟡 — у textarea «Сообщение» нет связи `label/for`
- **Где:** «Написать команде», блок «Сообщение».
- **Файл:** `index.html` (`<label>` без `for`).
- **Шаги:** axe / Lighthouse a11y.
- **Ожидаемо:** label привязан к textarea.
- **Фактически:** label-orphan, screen reader не объявляет имя поля.

### BUG-29 🟡 — низкий контраст у `--muted` текста
- **Где:** подзаголовки секций, hint-тексты.
- **Файл:** `styles.css` → `--muted: oklch(0.72 0.015 60)`.
- **Шаги:** WCAG-проверка / Lighthouse a11y.
- **Ожидаемо:** контраст ≥ 4.5:1 для основного текста.
- **Фактически:** ~3.0:1 на светлом фоне — fail.

### BUG-30 🟡 — кнопки «−/+» в корзине не доступны клавиатурой
- **Где:** строка корзины, контрол quantity.
- **Файл:** `app.js` → `renderCart()` (`<span onclick=...>` вместо `<button>`).
- **Шаги:** Tab по корзине.
- **Ожидаемо:** focusable controls.
- **Фактически:** фокус проходит мимо «−» и «+».

---

## 6. Производительность (2)

### BUG-31 🟡 — необрезанный scroll-listener
- **Где:** глобально, на каждом скролле.
- **Файл:** `app.js` → `window.addEventListener('scroll', ...)`.
- **Шаги:** прокрутить страницу при открытом DevTools.
- **Ожидаемо:** throttling / отсутствие лога в проде.
- **Фактически:** на каждый scroll-event пишется `console.log` —
  засорение консоли и небольшой CPU-spike.

### BUG-32 🟡 — синхронный цикл в калькуляторе на каждое изменение
- **Где:** «Калькулятор», движение ползунков.
- **Файл:** `app.js` → `runCalc()` (цикл `for ... 600000`).
- **Шаги:** быстро двигать ползунок «Чашек в день».
- **Ожидаемо:** мгновенный отклик.
- **Фактически:** заметная задержка/jank.

---

## 7. Локализация (2)

### BUG-33 🟢 — кнопки «Subscribe» и «Buy now» не переводятся
- **Где:** Hero (вторая CTA), форма Newsletter (submit-кнопка).
- **Файл:** `index.html` — у этих кнопок отсутствует атрибут `data-i18n`.
- **Шаги:** переключить язык на RU.
- **Ожидаемо:** русский текст.
- **Фактически:** остаются английскими.

### BUG-34 🔴 — символы валют перепутаны при смене языка
- **Где:** все цены (товары, тарифы, калькулятор, корзина).
- **Файл:** `app.js` → `currencySymbol()` (RU → `$`, EN → `₽`).
- **Шаги:** переключить RU↔EN.
- **Ожидаемо:** RU → `₽` (или `€`), EN → `$`.
- **Фактически:** значения «$/₽» меняются местами, и сумма не
  пересчитывается по курсу.

---

## 8. Состояния (1)

### BUG-35 🟡 — пустая корзина без empty-state, checkout активен
- **Где:** модалка корзины.
- **Файл:** `app.js` → `renderCart()` (нет блока «корзина пуста»);
  кнопка `#open-checkout` всегда активна.
- **Шаги:** очистить корзину → «Корзина» → «Оформить заказ» → заполнить
  чекаут → «Оплатить».
- **Ожидаемо:** empty-state с CTA «продолжить покупки», disabled на checkout.
- **Фактически:** заказ оформляется на $0.00, в `console.log` уходит
  пустой `items: []`.

---

## Сводная таблица

| #  | Категория       | Файл           | Уровень |
|----|-----------------|----------------|---------|
| 01 | UI              | index.html, app.js | 🟢     |
| 02 | UI              | index.html     | 🟢      |
| 03 | UI              | styles.css     | 🟡      |
| 04 | UI              | styles.css     | 🟡      |
| 05 | UI              | index.html, styles.css | 🟡 |
| 06 | UI              | app.js         | 🟢      |
| 07 | Validation      | app.js         | 🟡      |
| 08 | Validation      | index.html, app.js | 🟡  |
| 09 | Validation      | app.js         | 🟡      |
| 10 | Validation      | index.html     | 🟡      |
| 11 | Validation      | app.js         | 🟡      |
| 12 | Validation      | app.js         | 🔴      |
| 13 | Validation      | index.html, app.js | 🟡  |
| 14 | Validation      | app.js         | 🟡      |
| 15 | Logic           | app.js         | 🟡      |
| 16 | Logic           | app.js         | 🔴      |
| 17 | Logic           | index.html     | 🟢      |
| 18 | Logic           | app.js         | 🟡      |
| 19 | Logic           | app.js         | 🟡      |
| 20 | Logic           | app.js         | 🔴      |
| 21 | Security (XSS)  | app.js         | 🔴      |
| 22 | Security        | app.js         | 🔴      |
| 23 | Security        | app.js         | 🔴      |
| 24 | Security        | index.html     | 🟡      |
| 25 | Security        | index.html     | 🔴      |
| 26 | a11y            | index.html     | 🟢      |
| 27 | a11y            | index.html, app.js | 🟡  |
| 28 | a11y            | index.html     | 🟡      |
| 29 | a11y            | styles.css     | 🟡      |
| 30 | a11y            | app.js         | 🟡      |
| 31 | Performance     | app.js         | 🟡      |
| 32 | Performance     | app.js         | 🟡      |
| 33 | i18n            | index.html     | 🟢      |
| 34 | i18n            | app.js         | 🔴      |
| 35 | State           | app.js         | 🟡      |

**Итого: 35** · 🟢 7 · 🟡 21 · 🔴 7
